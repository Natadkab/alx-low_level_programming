#include "main.h"
/**
 * add - Adds two values
 * @n1: the first number to to be computed
 * @n2: the second number to be computed
 * Return: Integer value
 */
int add(int a,int b)
{
	int c = a + b;
	return (c);
}
